# v65 — полный аудит цепочки отправки актов

Исправлено:
- отдельный безопасный тестовый режим актов (ACTS_TEST_DEAL_ID, по умолчанию 38072; массово только ACTS_ALL_DEALS=true);
- `/config.js` больше не отдаёт BITRIX_WEBHOOK_URL и WAZZUP_CRM_KEY в браузер;
- файл ищется: вложение задачи → результат задачи (`tasks.task.result.list`) → task chat;
- `insufficient_scope` IM показывается как отдельная причина, а не «файла нет»;
- enum поля «Предпочитаемый способ связи» расшифровывается через `crm.deal.userfield.list`;
- Wazzup не переключается на другой канал, если выбранный канал не настроен;
- Email отправляется с `COMPLETED=Y`;
- телефон/email: основной контакт → компания;
- повтор после временной ошибки не теряется; 5xx Wazzup не ретраится автоматически во избежание дублей;
- отдельные маркеры текста/файла предотвращают повтор текста при повторной попытке файла;
- прямой обработчик проверяет фактическую стадию «Сделано», даже если ACTS_DONE_STAGE_ID не задан вручную.
